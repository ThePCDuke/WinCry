namespace WinCry.Services
{
    public enum ServicesOption
    {
        Nothing,
        Disable,
        Enable,
        Delete,
        Restore,
        Backup,
        Start,
        Stop,
        Update
    }
}
